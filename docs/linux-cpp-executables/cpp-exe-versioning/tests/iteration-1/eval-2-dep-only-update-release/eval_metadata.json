{
  "eval_id": 2,
  "eval_name": "dep-only-update-release",
  "prompt": "Time to release fooapp at <repo>. Since the last release the only thing that happened is that we dropped in an updated build of the vendored libfoo — no source changes at all. What should the new version be? Generate the release artifacts as well.",
  "assertions": [
    "New version is 0.9.2 — dependency change with zero app-code impact is a PATCH, not MINOR/MAJOR",
    "dependency.md line 1 is exactly 0.9.2+<count>.g<hash>",
    "dependency.md snapshot shows libfoo at 1.4.0 (same SONAME libfoo.so.1, version from soname/realname)",
    "Release note uncommitted (gitignored), CHANGELOG.md committed with 0.9.2 entry",
    "Release commit tagged v0.9.2"
  ]
}
